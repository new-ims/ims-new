# Next Steps
- [X] Configuration Model
  - [X] the ability to define optional enabled, disabled, readonly overrides in process level
  - [X] ability to define enabled, disabled, readonly in step level
- [ ] Process Store Build VM should receive all these optional functions in the config paramater
- [ ] Modify the enabled/ disabled/ readonly logic to add these optional overrides in the following specifity:
  - [ ] default logic (global)
  - [ ] process level overrides
  - [ ] step level overrides
  - [ ] Explicit Overrides (already implemented)

